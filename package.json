{
  "name": "three-js-template-lite",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.html",
  "targets": {
    "main": false
  },
  "scripts": {
    "clean": "rm dist/*",
    "dev": "parcel src/index.html --open --no-source-maps",
    "build": "rm dist/* && parcel build src/index.html --no-source-maps --no-scope-hoist"
  },
  "author": "Acid7",
  "license": "MIT",
  "dependencies": {
    "dat.gui": "^0.7.7",
    "glsl-noise": "^0.0.0",
    "three": "^0.128.0"
  },
  "devDependencies": {
    "@parcel/transformer-glsl": "^2.0.0-beta.2",
    "@parcel/transformer-stylus": "^2.0.0-beta.2",
    "glslify-bundle": "^5.1.1",
    "glslify-deps": "^1.3.2",
    "stylus": "^0.54.8"
  }
}
